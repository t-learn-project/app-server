version: '3.3'
services:
  django-backend:
    build: .
    command: "gunicorn core.wsgi:application --bind 0.0.0.0:8000"
    ports:
      - "8000:8000"
    env_file:
      - "./.env.prod"
#    volumes:
#      - static_volume:/home/app/tlearn/static


